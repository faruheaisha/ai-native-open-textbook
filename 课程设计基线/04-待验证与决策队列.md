# 待验证与决策队列

## 已完成的启动基线

- [x] 选择首个试点卷：卷 08 AI Agents & Agentic Workflows。
- [x] 为试点卷选择 3–5 个高质量、相互独立的来源：Hugging Face、Anthropic、Microsoft、Datawhale、OpenAI。
- [x] 给每个来源建立 Source Record：官方入口、作者/组织、版本、许可证、引用与媒体权利、最后核验日期。见 `research/08-agents/sources/`。
- [x] 完成一个 Chapter Evidence Packet，并明确 C0–C3 综合等级。首章 `CEP-08-01` 为 C2。
- [x] 产出设计级 Case Asset Pack，来源、输入、预期过程、输出、失败和验收可追溯。当前为 EVL-1，见 `research/08-agents/cases/CASE-08-01-CALENDAR.md`。
- [ ] 决定项目正式名称、仓库路径和公开许可证。
- [x] 冻结最终产品范式：Living Web + Editioned Bluebook PDF + Open Repository，共享单一内容真源。
- [x] 冻结制作顺序与全集目标：先卷 08，再卷 04；完成 14 卷后统一为一个网站、14 本独立 PDF、系列指南和全集合订本。

## P0：上游课程迁移（2026-09-10 起）

- [x] 建立 upstream 管理规约（命名、pinned commit、许可、刷新 SOP）：`upstream/README.md`。
- [x] 迁入并刷新 Hugging Face Agents Course（`b3946b1`，7 语言全量）。
- [x] 迁入 Hello-Agents 正文与代码（`4f7682c`，媒体排除）。
- [x] 续传 Microsoft AI Agents for Beginners：19 课正文与中文翻译（`25b7985`，411 个中文文件，zh-CN/HK/MO/TW 四变体）。
- [x] 续传 Anthropic Courses：`tool_use` 与 `prompt_evaluations`（`f4dbb13`，7 阶段完成）。
- [x] 建立上游课程台账与卷 08 迁移映射（复用模式与缺口分析）。
- [x] 清理失败传输残留的 `tmp_pack_*` 临时文件（2026-09-10 完成，清除只读属性后删除，释放 207.9MB）。

## P0：真实案例集迁入（2026-09-10 起）

- [x] 迁入 WorkBuddyGuide（`workbuddy.homes`）社区蓝皮书与 7 个投稿案例快照：MIT，`upstream/04-work/workbuddy-guide/`（`814ec83`，58 个文件）。
- [x] 迁入豆包工作蓝皮书（`doubaowork.homes`）内容快照：58 个节点（49 篇正文），`upstream/04-work/doubaowork-bluebook/`。
- [x] 更正豆包工作蓝皮书权利结论：站点对应公开仓库 `AlephAITech/DoubaoWorkGuide`，许可 MIT；快照已锚定 commit `ad7338e`，两份 JSON 与仓库 Git blob 逐字节一致（2026-09-10 复核）。
- [x] 建立两份 Source Record：`research/04-work/sources/SRC-WORKBUDDY-GUIDE.md`、`SRC-DOUBAOWORK-BLUEBOOK.md`。
- [ ] 为 7 个投稿案例各建 Case Asset Pack 候选卡片（含 EVL 与复现计划）。
- [ ] 核验两份案例集的媒体与个人数据权利：投稿封面图与截图、豆包品牌标志（归字节跳动）、作者名片（无来源记录）、正文媒体（无逐项来源 URL）。
- [x] 豆包工作蓝皮书授权确认：正文与站点代码为 MIT，可进入 CURATE + ADAPT 评估；媒体维持 INDEX，直到逐项核权完成。

## P0：当前制作主线

- [x] 完成卷 08 全卷 Knowledge Matrix、候选章节证据包队列和跨卷边界表。见 `research/08-agents/卷08-Knowledge-Matrix.md`。
- [x] 建立 Canonical Content Corpus、14 卷正文总索引和卷 08 卷首。
- [x] 完成卷 08 第 1–2 章 Evidence Packet 与读者正文完整草稿。
- [ ] 完成卷 08 Canonical Case 本地复现，将首章案例从 EVL-1 升到 EVL-5。
- [ ] 用卷 08 首章生成首个网站页与 PDF 样章，验证双载体竖切。
- [ ] 卷 08 全卷通过 Definition of Done，发布独立蓝皮书与在线版。
- [x] 卷 04 调研设计完成：办公任务地图、PKM 生命周期、来源课程矩阵、20 章 CEP 队列与 Canonical Case 候选，见 `research/04-work/卷04-调研设计.md`。
- [ ] 卷 04 首章 Evidence Packet（CEP-04-01）。
- [ ] 卷 04 全卷通过 Definition of Done，发布独立蓝皮书与在线版。

### P0-B：第二批开放课程与教程收录（2026-09-10 起）

- [x] 收录 Microsoft Generative AI for Beginners（MIT，`c9657f3`）：19 课英文正文 + 代码样例，`upstream/01-foundations/microsoft-generative-ai-for-beginners/`。
- [x] 收录 Anthropic Cookbook（MIT，`a97b9a2`）：600 文件 recipe 库，`upstream/08-agents/anthropic-cookbook/`。
- [x] 收录 OpenAI Agents SDK（MIT，`83c737f`）：1588 文件（源码 + docs + examples），`upstream/08-agents/openai-agents-python/`。
- [x] 收录 鱼皮 ai-guide（文档 CC BY-NC-SA 4.0 / 代码 MIT，`539082c`）：748 文件，`upstream/07-coding/liyupi-ai-guide/`。
- [x] 收录 Vibe Coding CN（MIT，`9b42dd1`）：1122 文件，`upstream/07-coding/vibe-coding-cn/`。
- [x] 收录 Qclaw（Apache-2.0，`c494768`，作者秋芝2046）：576 文件，`upstream/11-personal-agents/qclaw/`。
- [x] 六份快照均完成 Git blob 校验，并建立 Source Record 与台账。
- [ ] Anthropic `anthropics/courses` 剩余子课程补齐：`anthropic_api_fundamentals`、`prompt_engineering_interactive_tutorial`、`real_world_prompting`（当前仅 `tool_use` + `prompt_evaluations`）。
- [ ] Datawhale `easy-vibe`（19.3k★）、`vibe-vibe`（6.0k★）、`hello-claw`（2.2k★）：仓库未声明许可，先按 INDEX 处理，联系作者确认授权后再评估收录。
- [ ] 六份新快照的媒体与截图逐项核权（如需进入发布物）。
- [ ] 将新来源映射进卷 01、07、08、09、11 的章节证据包队列。

### P0-B2：第三批收录（Vibe Coding / Agent / Harness / Context / 办公 Agent，2026-09-10 起）

- [x] 收录千问办公绿皮书 QwenWorkGuide（MIT，`002f698a`）：599 个文本文件，`upstream/04-work/qwenwork-guide/`；blob 抽样 400 项全一致。
- [x] 抓取千问办公学习站「职场AI三千问」（未声明许可）：35 个 HTML + 51 案例数据文件，`upstream/04-work/qwenwork-learn-station/`；按 INDEX 处理。
- [x] 登记千问办公实战绿皮书 qwenwork.homes / `vaughan0855/qwenwork-bluebook`（无 LICENSE）：只建 Source Record，不镜像。
- [x] 收录 Stanford CS146S 中文版（MIT，`0d65f36f`）：188 文件（含 8 周作业），`upstream/07-coding/cs146s-cn/`。
- [x] 收录 AI Coding / Agent 工作流中文实战教程（MIT，`7a7c21b8`）：55 文件，`upstream/07-coding/ai-coding-guide-zh/`。
- [x] 收录 AI 编程 CLI 教程（MIT，`d187dbdb`）与 AI 工程从零到一中文版（MIT，`109181ce`）。
- [x] 收录 awesome-vibe-coding（CC0，`59d50281`）。
- [x] 收录 AI 智能体速成指南（MIT，`ea7f28ff`）：33 章 + 2 个实战项目，`upstream/08-agents/ai-agents-from-zero/`。
- [x] 收录 learn-claude-code（MIT，`0dcafa2a`）：17 步 nano harness 教程。
- [x] 收录 claude-code-best-practice（MIT，`2d6ea151`）、learn-harness-engineering（MIT，`77e7a3e2`）、how-claude-code-works（MIT）、codex-cli-best-practice（MIT）、codex-orange-book（MIT，`6c72add9`）等 Harness 线来源。
- [x] 收录 Context 线来源：Prompt-Engineering-Guide（MIT）、context-engineering-intro（MIT，`a2d84b02`）、agent-skills（MIT）、Awesome-Context-Engineering（MIT）、Agent-Skills-for-Context-Engineering（MIT）、Awesome-MCP-ZH（MIT）、ai-engineering-hub（MIT）。
- [x] 收录个人 Agent 线来源：awesome-openclaw-usecases（MIT）、awesome-openclaw-skills（MIT）、build-your-own-openclaw（MIT）、awesome-openclaw-tutorial（GPL-3.0，仅 INDEX）。
- [x] 建立批次 Source Record：`research/{07-coding,08-agents,10-context-memory,11-personal-agents}/sources/SRC-BATCH-*-2026-09-10.md`。
- [x] 建立社区与技术博客检索记录：`research/检索记录/社区与技术博客-2026-09-10.md`。
- [ ] 为以下来源补建独立 Source Record：`ai-coding-guide-stormzhang`、`ai-engineering-from-scratch-zh`、`awesome-vibe-coding`、`ai-agents-from-zero`、`datawhale-agentic-ai`、`learn-harness-engineering`、`how-claude-code-works`、`codex-orange-book`、`Prompt-Engineering-Guide`、`context-engineering-intro`、`agent-skills-addyosmani`、`awesome-mcp-zh`、`awesome-openclaw-usecases`。
- [ ] 逐项运行 Git blob 全量校验并修复失败项（见各台账“校验”行）。
- [ ] 联系未声明许可来源的作者确认授权：`learn.qwenwork.host`、`qwenwork.homes`、`datawhalechina` 三个仓库（easy-vibe / vibe-vibe / hello-claw）、`humanlayer/advanced-context-engineering-for-coding-agents`。
- [ ] 用统一任务集复现 3–5 条“办公 Agent 横评”结论，形成本项目自己的独立评测记录。
- [ ] 核对千问办公 FAQ 中的权限、积分与数据安全表述与官方帮助中心是否一致。
- [ ] 评估 GPL-3.0 来源（`awesome-openclaw-tutorial`）与 CC-BY-SA-4.0 来源（`claude-code-ultimate-guide`）的使用边界。

## P1：课程架构研究

- [ ] 对 14 卷标题逐项回查成熟课程和官方术语，确认最终英文名与中文译名。
- [x] 建立跨卷 Canonical Concept Registry 初版：Task Specification、Context、Verification、Tool、Skill、MCP、Memory、Agent Loop、Eval、Harness、Workflow、Artifact 等；随章节继续验证和扩展。
- [x] 为 14 卷建立调研设计与 Knowledge Matrix 骨架：每卷含 Volume Contract、Outcomes、候选来源、CEP 队列、Canonical Case 与跨卷接口，见 `research/README.md` 总览；卷 08 为完整矩阵。
- [ ] 逐章深化各卷 Knowledge Matrix 至可写作粒度（随制作波次推进，先卷 08、04）。
- [ ] 明确卷 09 Harness 与卷 10 Context/Memory/Extensions 的边界，避免重复。
- [ ] 明确卷 05 AI for Science 与卷 14 AI Scientist/Frontier 的边界。
- [ ] 明确卷 06 数字人与卷 11 Digital Persona 的边界。

## P1：资源事实重新核验

以下线索均来自上游讨论，进入正文前必须回到原始来源：

- [ ] Datawhale Happy-LLM、Hello-Agents、self-llm、Handy-Ollama、llm-deploy、unlock-deepseek、reasoning-kingdom 的当前版本、内容范围与许可证。
- [x] Hugging Face Agents Course 当前课程结构、中文支持、项目/证书/评测机制（2026-09-10 经本地快照 `b3946b1` 核验：unit0–unit4 与 3 个 bonus unit，zh-CN 与英文逐文件对齐，含最终项目与证书机制）。
- [ ] Microsoft 两套 Beginners 课程当前目录、维护状态、许可证与 CI/安全实践。
- [ ] Claude Design、Claude Science、ChatGPT Work、Codex Science 等名称与产品边界。
- [ ] Qoder CN 产品族（CLI/TUI、IDE、Quest、插件、云 Agent、SDK）的正式命名、平台支持与区域规则。
- [ ] ZCode、MiniMax Code、Qwen Code、DeepSeek Harness、Pi、OpenCode 的当前状态与教学价值。
- [ ] WorkBuddy、QwenWork、豆包工作、Manus 的官方案例库、媒体权利与真实工作链。
- [ ] LibTV、Lovart、MiniMax Design、ComfyUI、Figma MCP、Open Design 的产品/项目身份、官方教程和可下载资产。
- [ ] GPT-SoVITS、LivePortrait、OpenAvatarChat 等数字人组件的代码与模型许可证差异。
- [ ] Ollama、MLX、llama.cpp、vLLM、SGLang 的安装、硬件、平台与集成文档。
- [ ] OpenClaw、Hermes 等长期 Agent 系统的当前维护状态、安全边界与社区接受度。
- [ ] GPT-Red、Codex 安全能力、Trivy 等安全案例的准确名称和适用范围。

## P1：许可与开放发布

- [ ] 课程正文、代码、图片、视频、GIF、第三方附件分别确定许可证策略。
- [ ] 对 CC BY-NC-SA、MIT、Apache-2.0、GPL、研究许可证、未声明许可建立处理矩阵。
- [ ] 区分公开链接、内部研究快照、可重新分发资产、可改编资产。
- [ ] 建立 attribution 自动检查和第三方素材清单。

### P1-B：许可红线清单（2026-09-10 实读结论）

以下来源经 `LICENSE` 实文核验后判定为**不可迁入、不可改编**，只能作为 INDEX 或读文引用。所有条目须在 `research/*/sources/` 保留实读证据。

| 来源 | 实际许可（实读） | 红线 | 处置 |
|---|---|---|---|
| `NirDiamant/GenAI_Agents` | 自定义许可：仅限非商业；商业权利由作者独占保留；贡献者亦不得商用 | NonCommercial + 禁止再许可 | INDEX only |
| `datawhalechina/happy-llm` | CC BY-NC-SA 4.0 | NonCommercial + ShareAlike | INDEX only |
| `datawhalechina/llm-cookbook` | 无 LICENSE 文件（目录实读确认） | 未声明；且内容基于吴恩达 DeepLearning.AI 课程 | INDEX only，上游版权未转移 |
| `xianyu110/awesome-openclaw-tutorial` | GPL-3.0 | Copyleft，改编与分发受传染性约束 | INDEX（原已登记） |
| `FlorianBruniaux/claude-code-ultimate-guide` | CC-BY-SA-4.0 | ShareAlike，改编需同许可发布 | 快照保留，改编需单独评估 |
| `Tencent/workbuddy-bench` | NOASSERTION，且声明限制欧盟地区使用 | 地域限制条款 | INDEX（原已登记） |

- [ ] **决策点**：本项目是否接受非商业（NC）来源？若未来可能商业化，NC 类来源连改编都不可做，须在教材正文中完全排除并改走自写路径。
- [ ] 复核 GitHub 许可徽章与 `LICENSE` 实文不一致的情形（本轮实例：`rasbt/LLMs-from-scratch` 徽章 NOASSERTION，实文为 Apache-2.0）——一律以实文为准并留证。
- [ ] 把上述红线同步到 `02-资源与案例地图.md` 的「只登记不收录」清单（已同步第三批条目）。

## P2：动态网站与数据层

- [ ] 设计 Stable Core / Current Practice / Frontier Snapshot 的发布与更新机制。
- [ ] 为 benchmark、价格、福利、版本、下载方式建立 `last_verified` 和 `review_due`。
- [ ] 网站 IA 候选：Start、Roadmap、Learn、Labs、Explore、Resources、Updates。
- [ ] 建立统一组件：ConceptBlock、SourceCard、SourceView、Comparison、OfficialCase、VerifiedCase、DemoGIF、VideoDemo、ArtifactPreview、TaskSpec、FailureCase、SafetyBox、FreshnessBadge。
- [ ] 研究交互表达：Implementation Tabs、架构切换、时间线、工作流播放、证据展开、动态模型地图。
- [ ] 以卷 08 首章制作双载体竖切样板：同一内容源生成网页与 PDF 样章，验证交互降级、引用、图表和版本一致性。
- [ ] 冻结网站技术栈、PDF 排版栈、部署平台、域名和构建流水线。
- [ ] 设计系列首页、单卷 Landing Page、章节阅读页与蓝皮书封面系统。
- [ ] 建立 PDF 发行清单：edition、source commit、evidence cutoff、checksum、历史版本与在线 errata。

## P2：发现与反信息茧房

- [ ] 建立 Publisher Graph 与重点组织名单。
- [ ] 建立 Reference Graph，记录“从哪个高质量来源发现了谁”。
- [ ] 设计季度 Unknown Discovery Review。
- [ ] 为公众号、X、知乎、小红书、Bilibili、YouTube 等建立合规的索引/人工导入方式，不把绕登录、反爬和批量镜像作为基础设施。

## 暂不决策

- 最终每卷章数与总字数。
- 是否把网站、教材和资源注册表放在同一仓库。
- 是否对所有官方案例做团队复现。
- 是否建立证书、排行榜或课程积分。
- 是否把任何当前“最强产品/模型”写进长期教材。

## 补记：第三轮检索引入的 P0 / P1 项（2026-09-10）

> 本节内容按优先级归入相应等级，位置在文末以便与第一轮记录对照。

### P0（阻断正文写作）

- [ ] **取得 OpenAI `harness-engineering` 原文**（`openai.com/index/harness-engineering/`，2026-02-11，Ryan Lopopolo）。
  现状：本机对 openai.com 持续返回 **403**，正文无法落盘；六大概念（仓库即记录系统 / 地图而非手册 / 机械化执行 / 智能体可读性 / 吞吐量改变合并理念 / 熵管理）目前**全部为转述**。
  影响：卷 09 的全部核心概念都建立在这一转述之上。**在取得原文前，卷 09 的任何概念表述都不得标为"已验证"。**
  可选通道：境外网络、官方 RSS、授权转载、或经 `lopopolo/harness-engineering` 仓库正文交叉确认（作者同名）。

### P1

- [ ] **P1-C：是否接受 CC BY-NC-SA 4.0 来源？**（升级自既有决策点；2026-09-10 第四轮后**升级为 P0-B**，见文末补记）
  受影响来源（本轮新增计数）：`datawhalechina/easy-vibe`（19,358★）、`datawhalechina/vibe-vibe`（6,023★）、`datawhalechina/self-harness`（250★）、`AlexAnys/agent-first-dev`、`jayminwest/agentic-engineering-book`、`ranxi2001/zero2Agent` 的绿皮书 PDF。
  **影响面**：中文侧最热的 vibe coding 与 harness 课程全部在 NC 之下。若本项目不接受 NC，则卷 07 / 08 / 09 的**中文主线需要另建**。
  建议：尽快决策并在 `02-资源与案例地图.md` 固化。

- [ ] **P1-D：CC BY-SA 4.0 的传染性边界。**
  新增受影响来源：`zjp1997720/zhijian-ai-bluebook-workbuddy-harness`（正文 CC BY-SA 4.0 / 代码 MIT / 品牌资产保留）。
  决策内容：卷 04 若改编其正文，衍生作品须以 CC BY-SA 4.0 发布 —— 与当前卷册许可策略是否兼容？不兼容则限定为**转述引用**。
  既有同类：`FlorianBruniaux/claude-code-ultimate-guide`（CC-BY-SA-4.0）、`xianyu110/awesome-openclaw-tutorial`（GPL-3.0）。

- [ ] **P1-E：抓取并核验两份厂商官方 Skills 目录。**
  `openai/skills`（26,843★）与 `microsoft/skills`（3,006★）—— 未核许可、未抓取。对卷 10 价值高。

- [ ] **P1-F：卷 09 与卷 10 的边界重画。**
  Fowler 原文（已落盘）明确把 **context engineering 定位为 harness engineering 的关系项**，而本项目按"卷 09 讲机制、卷 10 讲策略"平行切分。需在两卷 Volume Contract 中各写一句显式边界声明。

- [ ] **P1-G：`microsoft/AI-Engineering-Coach` 的 45 条反模式规则需独立复核。**
  这是**厂商观点**，不是同行评审结论。若进正文，须注明来源，并由本项目逐条判断是否成立。

### P2

- [ ] **P2-H：`Qwenwork-MKT`（疑似官方营销组织）仓库群定位未确认。**
- [ ] **P2-I：Stanford CS146S 官方作业仓库无 LICENSE** —— 能否引用其作业结构待定；中文版 `cs146s-cn` 的课程内容版权同样归原课程方。
- [ ] **P2-J：`openai/skills` 与 `microsoft/skills` 之外，是否存在其他厂商官方 Skill 目录**（如 Google / AWS）。

### 已关闭

- [x] `superpowers` / `mattpocock-skills` 的不可解析路径：确认为**符号链接**（`AGENTS.md → CLAUDE.md`），已由 `upstream/_fix-symlinks.ps1` 补全，两来源现均 **PASS**。
- [x] `hello-agents` 的 `HelloAgent_fastgptCase.json` 与 `第三章 大语言模型基础.md` 与 pinned commit 不一致：已按 `4f7682ce` 重取，字节数与上游一致。
- [x] `.gitignore` 系统性缺失：已补 filter 并回填 23 个来源。

## 补记：第四轮检索引入的 P0 / P1 / P2 项（2026-09-10）

### 已关闭

- [x] **P1-E：抓取并核验两份厂商官方 Skills 目录。**
  `openai/skills`（688 文件，44 个技能目录；**逐技能 `LICENSE.txt`**，抽样 Apache-2.0）与 `microsoft/skills`（1,900 文件；**MIT**；含 `.claude` / `.opencode` 多宿主目录）均已迁入并校验 `bad=0`。见 `upstream/10-context-memory/上游资源台账.md` 第四批节。

### P0（阻断正文写作）

- [ ] **P0-B：中文 vibe coding 课程的 NC 许可决策**（由 P1-C 升级）。
  受影响来源：`datawhalechina/easy-vibe`（19,358★）、`datawhalechina/vibe-vibe`（6,023★）、`vibefast-app/vibefast-docs`、`liyupi/ai-guide`（19,790★）、`datawhalechina/deepagents-in-action`（1,992★，内容部分）。
  影响：卷 07 / 08 / 09 / 10 的**中文主线来源集合**。若拒绝 NC，则中文侧需要自写单元或改用英文 MIT 来源。
  建议：与项目许可策略（是否商业化）合并决策，一次定案。

- [ ] **P0-C：官方文档站点快照的引用规则（新）。**
  本批引入 5 个官方文档站点（千问办公 / Manus / 扣子 / Dify / CodeBuddy），均受各自站点条款约束。需要固化的规则：引用三要素 = **URL + 抓取日期 + 端点（llms.txt / .md / 页路径）**；快照仅作研究底本，不镜像到发布物；产品细节属 Live Facts，引用前回官方页面核验。

### P1

- [ ] **P1-H：ChatGPT Work 官方文章定位。** `help.openai.com` 可达（200），但尚未定位对应文章；`openai.com` 主站仍 403。
- [ ] **P1-I：千问办公官方口径与社区来源的交叉核验。** 对象：`qwenwork-guide`（绿皮书）、`qwenwork-learn-station`、`zhijian-ai-bluebook`。产品入口、额度（网盘 1/10/20 GB）、席位与积分规则均易漂移。
- [ ] **P1-J：扣子官方文档补全。** `docs.coze.cn/llms.txt` 索引 958 条，本轮只取 100 篇核心页。
- [ ] **P1-K：`Tencent/workbuddy-bench` 细读。** 自定义 Tencent 许可（声明不适用于欧盟）+ 80 个 repo 级 SWE 任务、18 类角色；引用边界需先明确。
- [ ] **P1-L：三个产品缺口的获取路径。** Genspark（403，需换网络/代理）、Flowith（域名不可达）、豆包办公（SPA，无静态文档）。
- [ ] **P1-M：Harness 中文译名决策。** 社区并存"驾驭工程"（`wquguru/harness-books`）与"驭缰工程"（`deusyu/harness-engineering`）；卷 09 须固定一个并声明取舍。

### P2

- [ ] **P2-K：各产品内置技能对标表。** 以 `multi-platform-skills-archive` 的 5 个 README 索引为输入，自建"豆包办公 / WorkBuddy / 千问办公 / ChatGPT agent 技能与专家团对照"（只做事实对照，不复制其内容）。
- [ ] **P2-L：高星空壳仓库检测规则。** 迁入前先看 git tree（反例：`emarco177/langchain-course` 1,649★ / 4 文件），已写入检索纪律。

## 补记：第五轮检索引入的结论与决策项（2026-09-10）

本轮以**"能不能教"**准入，不以星数或数量准入。净增 22 条快照（20 仓库 + 2 文档站），入库后剔除 3 条内容量不足的仓库。总览见 [检索记录](research/检索记录/GitHub课程与Agent产品-第五批-2026-09-10.md)。

### 已关闭

- [x] **P1-M：Harness 中文译名决策 → 转为「叙事决策」并升级为 P0-D。**
  本轮取得决定性证据：译名分歧的实质不是翻译，而是**对 harness 职责的两种工程立场**。
  - 《御舆：解码 Agent Harness》（`09-harness/claude-code-book-yuyu`，15 章 + 4 附录，中英双语）以《周礼·考工记》"造车"正面使用"驾驭"隐喻：舆/辕/辐/軎辖/轼/御 → 运行时/对话循环/工具系统/权限管线/钩子/架构认知。
  - `10-context-memory/practical-guide-context-engineering` 明确**反对**"马具/驾驭"读法，理由是它把 agent 退回 workflow 时代（"骨架被搭建得太清晰，Agent 只能沿着骨架行走"），并提出替代定义：**"harness 定义的是边界和协作协议，而不是控制每一步的执行"**。
  - 因此"用什么译名"无法孤立决定 —— 先定立场，再定词。

### P0（阻断正文写作）

- [x] **P0-D：卷 09 概念章的立场选择（由 P1-M 升级）→ 已关闭（2026-09-10）。**
  **决策：不做取舍，两种读法并列呈现。** 完整论证见 [`research/09-harness/harness隐喻-两读并列.md`](research/09-harness/harness隐喻-两读并列.md)。
  - 读法 A（承载/驾驭）：《御舆：解码 Agent Harness》以《周礼·考工记》立论，映射舆=运行时、辕=对话循环、辐=工具系统、軎辖=权限管线、轼=钩子、御=架构认知。
  - 读法 B（周围执行系统/边界与协作协议）：OpenAI 官方博客原句 *"That surrounding execution system is the harness."*；Anthropic 三 Agent + sprint contract + 交接物；中文指南"定义边界和协作协议，而不是控制每一步的执行"。
  - 关闭理由：两读各有不同的一手支持者、都不空洞、且分歧本身有教学价值。**用户已指示不必为此忧虑。**
  - 卷 09 执行方案：把分歧本身作为概念章的教学内容（原第 3 种），并在章末声明本教材主叙述采用读法 B 的技术定义、保留读法 A 作中文叙事外壳。不写"公认定义"。

- [x] **P0-E：中文来源的转述链风险 → 大幅解除，降级为 P2（2026-09-10）。**
  **Anthropic 侧已完全解除**：`anthropic.com/engineering/*` 全站可达，`effective-harnesses-for-long-running-agents` 与 `harness-design-long-running-apps` 两篇原文已落盘（`upstream/09-harness/anthropic-engineering-blog/`），卷 09 可直接引用原文，**"经中文社区转述"标注在该两项上作废**。
  **OpenAI 侧部分解除**：`openai.com` 主站仍 403，但找到可达替代通道——
  - `developers.openai.com/blog/codex-as-a-platform.md`《Codex as a platform: build on the open agent harness》提供 **OpenAI 官方 harness 定义原文**（"That surrounding execution system is the harness."）与 ARC-AGI-3 量化数据；
  - `developers.openai.com/codex/**`（149 篇）、`cookbook/**`（49 篇）、`api/docs/**`（226 篇）、`plugins/**`（26 篇）全部为官方 Markdown 端点，已落盘。
  - **仍未得**：`openai.com/index/harness-engineering/` 这一篇本身（主站 403）。引用该篇专属的五步法时，**继续标注"经中文社区转述整理"**。
  - 沿用规则：原文可得者署原文；不可得者署转述。二者在同一章内必须可区分。

### P1

- [ ] **P1-N：`export-ignore` 来源的复核（新，工程性）。**
  全库 18 个来源含 `.gitattributes`，需逐个确认是否使用 `export-ignore`；已确认 1 个（`Chachamaru127/claude-code-harness`，600 选中 / 145 落盘）。受影响来源须在索引 `note` 标注「按 export-ignore 语义部分迁入」，且 **`MISS` 不得判为快照损坏**。

- [ ] **P1-O：`dsh` 快照补全（新）。**
  `deepseek-ai/deepseek-harness` 当前只覆盖 `.agents/notes/**`（上限 900 文件），不含 `apps/`、`packages/`、`docs/`（仓库共 10,319 blob）。若要引用其架构细节，须先补取 `docs/`。

- [ ] **P1-P：办公 Agent 三个缺口的投入决策（新）。**
  豆包办公（SPA，无机读端点）、Genspark（403）、Flowith（不可达）。卷 04 现有官方一手来源（千问办公两套 + 扣子 + Manus + Dify）是否已足够开写？若足够，则三缺口降级为"长期观察"，不再投入抓取。

- [ ] **P1-Q：`qwenwork.cn/docs` 的 Admin OpenAPI 是否升级为正式来源（新）。**
  103 个端点目前只登记不抓取。其"策略是独立实体"的结构判断（`connector-/skill-/model-/hook-/quota-/expert-suite-/trusted-device-policies`）对卷 12 有直接价值 —— 若卷 12 采用，则需按端点抓取代表性页面。

### P2

- [ ] **P2-M：`get-shit-done`（64,564★）的定位辨析。** 高星但本质为 meta-prompting / 规格驱动系统；宜作"提示词系统 vs 工程系统"的辨析样本，不宜作上下文工程教材引用。
- [ ] **P2-N：Bing 检索通道的稳定性。** 已确认可用（去 `setlang`/`cc`），但部分中文查询会间歇返回"降级页"（首条为字典/百科类结果），需按首条 URL 特征识别并换词重试。是否把该通道写成可复用脚本待定。
## 补记：第六轮｜Anthropic 与 OpenAI 官方技术文档提取（2026-09-10）

本轮突破点是**通道**，不是内容堆叠。此前两条长期阻断（Anthropic 全站不可达、OpenAI 主站 403）均找到可行的机读通道，一次性把两家厂商的一手技术文档引入项目。总览见 [检索记录](research/检索记录/Anthropic与OpenAI技术文档-第六批-2026-09-10.md)。

**净增 10 条快照，`_快照索引.json` 133 → 143。** 约 1,450 个新文件 / 约 100 MB。

收尾时又从 Claude Code 官方文档的"延伸阅读"里发现了 **`claude.com/blog`**（Anthropic 官方产品与工程博客，sitemap 3,151 条 URL）——该站 `llms.txt` 只是导航索引、无 `.md` 孪生地址，正文需 HTML 提取。**按教学价值选录 59 / 235 篇**，落在 `upstream/09-harness/anthropic-blog-official/`。未录清单可从快照内的 `_sitemap.xml` 复原。

### 通道突破（可复用）

1. **Anthropic**：`www.anthropic.com` 无 llms.txt，但有 `sitemap.xml`；`platform.claude.com` / `docs.anthropic.com` / `docs.claude.com` 三域镜像，均暴露 `llms.txt` + 逐页 `.md`；`code.claude.com/docs` 暴露 `llms.txt` + llms-full（**发现路径：官方文档页面自己写了 "Fetch the complete documentation index at…"**）。
   - **注意**：`www.anthropic.com` 对并发敏感，4 线程全败、2 线程 + 1500 ms 通过。
2. **OpenAI**：`openai.com` / `platform.openai.com` / `help.openai.com` 仍 403，但 **`developers.openai.com` 与 `learn.chatgpt.com` 是全站 Markdown 化文档站**——任意页面 URL 追加 `.md` 即得纯 Markdown；且各产品线单独暴露 `llms.txt` / `llms-full.txt`（站点总索引列出 11 个文档集入口）。`platform.openai.com/docs` 的 403 由此绕开。

### 已关闭

- [x] **P0-D（见上）：隐喻之争 → 两读并列，不做取舍。**
- [x] **P0-E（见上）：转述链风险 → Anthropic 侧完全解除；OpenAI 侧部分解除，仅 `openai.com/index/harness-engineering/` 一篇仍须标注转述。**

### P1

- [ ] **P1-R：`openai.com/index/harness-engineering/` 的最后一跳（新）。**
  该篇是术语普及的关键文献，主站 403。已排除：主站直连、`developers.openai.com/index/...`（404）、`blog/harness-engineering.md`（404）。**未排除**：主站 RSS/JSON 端点、`openai.com` 的其它子域镜像、该文在 `developers.openai.com/blog/llms-full.txt` 中是否已被改写收录（当前 grep 未见同名文章，但 `codex-as-a-platform.md` 已提供官方 harness 定义，**该项优先级可降**）。
  → 决策：是否继续投入？若 `codex-as-a-platform.md` 已足以支撑卷 09 的 OpenAI 侧论述，则本项降为长期观察。

- [ ] **P1-S：文档站快照的引用规则固化（由 P0-C 转化，收敛版）。**
  本轮新增 9 个文档站快照，引用规则需统一为三要素：**URL + 抓取日期 + 端点类型（官方 Markdown / HTML 提取文本）**。
  - 官方 `.md` 端点的快照（Claude Code 文档、Codex 文档、OpenAI 博客 / Cookbook / API / 插件、Anthropic 平台文档）：可直接引用。
  - **HTML 提取文本**的快照（仅 `anthropic-engineering-blog`）：**引用原句前必须回原文核对措辞**。
  - 全部为 `cite_only`，不进入 CURATE / ADAPT。

- [ ] **P1-T：Anthropic 11 个翻译版（各 249 页）的取用策略（新）。**
  英文 628 页已全量落盘；翻译版是英文子集。**默认不收**。若卷 04 / 卷 09 的中文读者需要中文术语对照，可按 `/docs/zh-CN/...` 现场取用，不入库。

### P2

- [ ] **P2-O：OpenAI `showcase`（78 条）与 `learn`（138 条）是否补抓（新）。**
  本轮只取索引与 llms-full，未抓逐页正文：showcase 是项目画廊、learn 以视频与文档链接为主，教学价值判断为低。**若卷 07 需要"真实案例集"的英文侧补充，再评估 showcase。**

- [ ] **P2-P：`openai-api-reference-en` 的 4 个 schema 转储页排除记录。**
  已按"质量优先"原则排除 4 个 15–23 MB 的机器生成 schema 转储页（`websocket-events` / `streaming-events` × `responses` 与 `beta/responses`），URL 与理由写在 `_快照信息.md`。是否需要在卷 09 的流式章节给出"如需完整事件 schema 请回原站"的提示，待定。

### 第六轮收尾：台账补齐与全量校验（2026-09-10）

- [x] **四份台账补齐第六批**：`upstream/09-harness/上游资源台账.md`、`upstream/08-agents/上游课程台账.md`、`upstream/11-personal-agents/上游资源台账.md`；`upstream/01-foundations/上游资源台账.md` **整体补账重写**（此前只登记 1 条，实际 8 条）。
- [x] **全量 Git blob 校验通过：`ok=40,930 / bad=0`**（18 个站点型快照按设计跳过）。报告：`research/检索记录/快照校验报告-2026-09-10.txt`。
- [x] **修复 Microsoft 课程 36 个内容不符文件**：根因是迁移通道给"原本无结尾换行"的文件多补了一个行尾符（本地恒比上游多 1 字节）。修复方式为**按上游 blob 字节逐个覆写**，非推测性删除。
- [x] **补迁该课程 8 个此前遗漏的非翻译文件**：根 `.gitignore`、4 个 `typescript/.gitignore`、**`LICENSE`**、`index.html`、`pyproject.toml`。**其中 `LICENSE` 最关键——许可判断不能只靠台账文字，快照里必须有许可原文。**
- [x] **校验脚本改进**：站点型快照不再只报"SKIP"，改为输出**文件数与体积**（`站点型快照 files=205 17.9 MB`），使快照漂移可见；`fetchmany.ps1` 的已知报错行已清理。

### P1（收尾新增）

- [ ] **P1-U：`claude.com/blog` 其余篇目的按需补抓（新）。** 本次只录 59 / 235 篇（按教学价值）。**未录清单已随快照保留（`_sitemap.xml` + `_选取清单.txt`）**，需要时按 slug 现取即可，**不必重新检索**。触发条件：某卷正文需要具体客户案例或某篇公告的一手数据。
- [ ] **P1-V：`claude.com/plugins`（336）与 `/connectors`（797）是否立快照（新）。** 两处均为目录型页面（条目卡片），本轮判定正文价值低而未收。若卷 10 / 卷 11 需要"插件与连接器生态"的规模证据，另立快照；否则保持只引用索引。
- [ ] **P1-W：校验脚本的 `missing` 分类输出（新）。** 本轮 `missing=94,065` 中绝大多数是**有意排除**的多语言翻译与媒体目录，但报告只给总数不分类，容易被误读为严重缺口。**应增加"按首层目录归类 missing"的输出。**
- [x] **P1-X：许可原文落盘核查（已执行，全卷）。** 触发点：`microsoft-generative-ai-for-beginners` 的 `LICENSE` 一直没迁。
  - **普查结果**：143 条快照中，**14 个仓库型快照的上游有 LICENSE 但本地没有**（`build-your-own-openclaw`、`ed-donner-agents`、`better-harness`、`workbuddy-bench-official`、`spec-kit`、`langchain4j-for-beginners`、`second-brain-ai-assistant-course`、`ed-donner-production`、`pocket-manus`、`microsoft-skills`、`edgeai-for-beginners`、`mine-context`、`strands-harness-sdk`（`LICENSE.APACHE`）、`meta-skill-harness-revfactory`）——**已全部补迁**，逐字节写入。
  - **根因**：tarball 通道的文件选择正则按**扩展名**判断文本文件，而 `LICENSE` 是**无扩展名**文件，被静默跳过。**已记为通道缺陷。**
  - **剩余 15 条**确认为"上游本来就没有根 LICENSE"（`未声明` / `README 声明` / `逐技能 LICENSE.txt` / `仅子目录有 LICENSE`），台账记录与事实一致，**不需要动作**。
  - **纪律**：凡正文或台账写"某仓库是 MIT / Apache-2.0"，必须能指向**快照内的许可文件**；只有 README 声明的，必须写明"README 声明，无 LICENSE 文件"。
## 补记：第七轮｜课程编排范式调研（2026-09-10）

背景：用户方向从"收集来源"转为"编排成一门真的读得下去的课"，点名对标花叔 Claude Code 橙皮书。关键词：有始有终、有价值、学得下去、高质、编排整理。

### 已关闭

- [x] **第七轮-A：花叔「橙皮书」系列解剖（新）。** 抓取系列页（60,231 B）+ 5 个卷页完整目录 + OpenClaw 卷 PDF（9,337,179 B / 114 页 / 78,007 字符提取文本），得到系列级、卷级、章级三层结构证据。
- [x] **第七轮-B：bozhouDev《ChatGPT 橙皮书》编排解剖（新）。** 使用本地已有快照（MIT），做全量标题树（h2=39 / h3=28 / h4=145 / h5=136）与量化特征分析（表格行 1,214 / 代码块 274 / 图片 123 / 含粗体行仅 61）。
- [x] **第七轮-C：产出编排规范文档（新）。** `research/publication/课程编排范式-橙皮书双样本.md`（37.4 KB / 667 行），含 18 条编排规律、五级落地契约（系列级/卷级/章级/案例级/学得下去机制）。
- [x] **第七轮-D：14 卷编排总表（新）。** 为每卷补「一句话承诺 + 毕业产物 + 承接关系」；已落到 `content/volumes/README.md`。
- [x] **第七轮-E：Source Record（新）。** `research/publication/sources/SRC-HUASHENG-ORANGE-BOOKS.md`。

### 已关闭（决策）

- [x] **花叔系列的许可状态判定。** 站点、卷页、PDF 内页**均无任何许可声明**；`robots.txt` 为 `Allow: /`。按课程宪法原则 15「Public does not automatically mean reusable」判为 **INDEX**：只作编排形式对标，正文不得改写、翻译或再发布。若作者日后公开许可，重新评估为 CURATE。

### P0（编排层，不依赖未决决策）

- [x] **P0-C：把编排契约写成模板（部分完成，2026-09-10）。** 已建 `templates/Volume-Contract.md`（卷首/卷尾 7 问契约）与 `templates/Chapter-Contract.md`（章首承诺与读法、八要素、结构载体配比、难度曲线）。**剩余**：把 §8.1 章首三要素与 §8.3 结构载体配比回写进 `05-网站与蓝皮书双载体出版范式.md` 的章节结构节。
- [ ] **P0-D：卷 08 章首三要素补齐（新）。** 第 1–2 章已有完整 15–16 节正文模板，缺「本章承诺 / 阅读成本 / 本章产物」三行。
- [ ] **P0-E：卷 08 Spine Case 推进表（新）。** 建议复用 `CASE-08-01-CALENDAR` 作为贯穿案例，定义它在第 1、3、4、8、12 章的生长轨迹与各章结束时的可运行状态。
- [ ] **P0-F：14 卷各补一行「模板资产」（新）。** 对应样本书的「任务模板库」——读者一年后回来只取这一份文件就能继续干活的资产。

### P1（新）

- [ ] **P1-Y：系列名与系列定位（仍阻塞）。** 编排规范 §6.1「系列封面六项」依赖此项；对应既有未决项"项目正式名称与公开许可"。
- [ ] **P1-Z：手册型卷是否允许（新）。** 卷 10、卷 13 在实际使用中更接近查阅型。是否允许这两卷采用"手册型分区"编排（对标花叔 OpenClaw 卷的 35 章 / 8 部结构），待决。
- [ ] **P1-AA：每章阅读成本的上限标准（新）。** 建议 25 分钟；需定义含代码块章节如何加权。
- [ ] **P1-AB：Spine Case 是否跨卷（新）。** 卷 07 与卷 08 若围绕同一项目，需定义"引用 + 增量"的具体写法。
- [ ] **P1-AC：视频配套是否进入本项目范围（新）。** 花叔每卷配 B站 / YouTube 完整教程；本项目当前仅文字载体。

### 编入台账

- 本轮新增 `upstream/` 文件：**0**（花叔系列按 INDEX 处理，不进快照区）。
- 新增研究文档 1 份、Source Record 1 份；更新 `content/volumes/README.md` 与 `课程设计基线/README.md` 导航。
## 补记：第八轮｜网页交互版设计规范（2026-09-10）

背景：用户要求"做网页交互版的课程，像 doubaowork.homes 一样，交互设计更成熟、视觉与案例展示更适配"。前七轮完成的是内容与编排层；本轮补齐**交互与呈现层**。

### 已关闭

- [x] **第八轮-A：两个真实站点的实现级解剖（新）。**
  - 样本 C `doubaowork.homes`：纯静态 SPA + 双层 JSON（`site-content.json` 466 KB 全文 / `site-index.json` 38 KB 生成式索引，每节点带 charCount / imageCount / videoCount / excerpt）。已到实现级：hash 路由、首帧前定主题 + `is-booting` 防闪白、`skip-link`、`role=combobox` 搜索、进度与偏好持久化、`ASSET-PROVENANCE.md` 素材溯源（含 SHA-256）、浏览器回归 + audit 脚本（含弱网首屏与横向溢出检查）。
  - 样本 D `workbuddy.homes`：VitePress（cleanUrls + 本地搜索 + lastUpdated + sitemap + Mermaid + 按页 SEO）；`sidebar.ts` 结构即课程结构（全展开）；**案例侧边栏从 frontmatter 自动生成并按日期排序**；`cases/*` 统一 `aside:false, outline:false` 宽版式；仓库内含一份 10,019 字符的公开 UI 设计规范（Design Token / 字体尺度 / 动效 / 断点 / 无障碍 / 实施映射 / 验收清单）。
- [x] **第八轮-B：产出交互规范。** `research/publication/网页交互版设计规范.md`（44.2 KB / 883 行 / 16 节）。
- [x] **第八轮-C：可吸收 / 改造 / 新增三分清单。** 16 项直接吸收、8 项改造吸收、6 项本项目新增。

### 已关闭（决策）

- [x] **技术选型冻结：VitePress 为骨架 + 自定义主题层与少量 Vue 组件。**
  理由：`content/` 是 Markdown 真源，VitePress 直接消费，不引入中间转换（课程宪法原则 17）；同语言同题材已有 MIT 验证案例；静态产物可离线镜像。**明确不做**：后端、登录、数据库、埋点、重型动画库、客户端渲染全文。
- [x] **首页不显示访问量统计。** 与"可信教材"调性不符且引入第三方依赖；改为显示内容统计（卷数 / 章节数 / 已发布 / 最后更新）。
- [x] **视觉方向冻结为「编辑型研究感」，并以「内容寿命三色」作为品牌信号。**
  三色 = 稳定核心（绿）/ 当前实践（琥珀，带核验日期）/ 前沿快照（紫），在目录、章标题、卡片、证据面板四处一致显示。这是本项目独有、两站都没有的视觉语言，同时服务教学（读者知道什么会过期）与品牌（一眼可辨）。
- [x] **不使用像素风格与像素字体。** 样本 D 的像素科技感适配工具手册，不适配带证据的长期教材。识别度改由**结构线语言**承担（全部由 CSS 与内联 SVG 生成，从根本回避素材许可问题）。
- [x] **案例库默认排序不按时间。** 按「证据等级降序 + 卷号」，因为教材关注可复现性而非新鲜度；需在页面上说明理由（与样本 D 的做法不同）。

### 本项目新增（两站都没有，已写入规范）

- [ ] **新增-1：证据层交互。** 行内标记 ▤ → 右侧证据卡片；字段含**分歧**（课程宪法要求"不偷偷消除分歧"）。待实现。
- [ ] **新增-2：案例回放 Trace Replay。** 时间轴 + 步骤轨道 + 状态变化 + 决策点 + 失败/人工介入标注；`trace.json` schema 未定。
- [ ] **新增-3：内容寿命标记**（四处一致显示）。
- [ ] **新增-4：诚实降级入口。** 每章/案例显示 CEP / EVL / 许可 / 审查 四项状态，禁止用视觉手段掩盖未完成。
- [ ] **新增-5：双载体语义一致性检查** `check-parity.mjs`。
- [ ] **新增-6：毕业产物展示位**（首页第 6 段"终点展示"）。

### P0（实现层，新）

- [ ] **P0-G：`scripts/build-index.mjs`。** 从 `content/volumes/**` 生成 `data/site-index.json`（14 卷树 + 每章 promise/minutes/sprints/charCount/evidencePacket/integration/cases/sources/stability/excerpt）。**`sidebar.ts` 与首页全部列表必须由它驱动，禁止手写章节列表。**
- [ ] **P0-H：Design Token 与全局样式。** 按规范 §9 建立颜色/字体/圆角/阴影/间距 Token，含深色模式三态与防闪白脚本。
- [ ] **P0-I：首页十段式。** Hero（14 卷书脊阵列主视觉）/ 价值条 / 内容统计 / 四条学习路径 / 按任务进入（七类）/ 终点展示 / 卷册目录 / 方法链 / 版本与证据说明 / 共创 CTA。
- [ ] **P0-J：章阅读页三栏 + 章元信息条。** 含寿命标记、分段读法表、上一篇/下一篇、继续阅读。
- [ ] **P0-K：卷 08 第 1 章完整竖切。** 首页卡 → 卷页 → 章页 → 证据面板 → 案例回放 → PDF 样章。

### P1（新）

- [ ] **P1-AD：`trace.json` schema 冻结。** 需与卷 08 首章案例的 runner 输出对齐后再定。
- [ ] **P1-AE：案例产物托管位置。** 站内 `public/cases/` 还是独立对象存储，取决于产物体量与许可。
- [ ] **P1-AF：搜索索引分片。** 14 卷全文检索是否需要按卷分片，需实测。
- [ ] **P1-AG：是否提供离线阅读包。** 作为 PDF 之外的第三种载体，成本与收益待评估。
- [ ] **P1-AH：PDF 生成管线选型。** 规范只定义网站侧；`print/` 侧构建器（Paged.js / PrinceXML / headless Chrome 等）另立文档。
- [ ] **P1-AI：品牌视觉与系列名一并定稿。** 视觉 Token 已给出建议方案，最终定稿阻塞于系列名未决项。

### 编入台账

- 本轮新增 `upstream/` 文件：**0**（两站快照已于此前迁入，本轮只做分析）。
- 新增研究文档 1 份（44.2 KB）；更新 `课程设计基线/README.md` 导航。

## 第九轮（2026-09-11）：网站编排与可上线交付

### 已关闭（决策）

- [x] **部署地点暂不预设。** 站点已具备上线条件，但托管方（Vercel / Netlify / Cloudflare Pages / 自建 nginx）未定，**当前只在本机运行**。`DOCS_HOST`、`DOCS_BASE` 的最终取值随托管方一并待定。
- [x] **域名未定时不写死绝对地址。** `scripts/build-index.mjs` 与 `site/docs/.vitepress/config.mts` 共用同一套探测顺序：`DOCS_HOST` → 平台注入变量（`VERCEL_PROJECT_PRODUCTION_URL` / `VERCEL_URL` / `URL` / `DEPLOY_PRIME_URL` / `CF_PAGES_URL`）→ 占位域名。**两处顺序必须保持一致**，否则 robots.txt 里的 sitemap 地址会和实际产物对不上。
- [x] **sitemap 排除 404 页。** 判据是 `!/(^|\/)404$/.test(url)`：`transformItems` 收到的 `url` 形如 `"404"`、`"cases/"`，**不带前导斜杠**；写成 `endsWith('/404')` 会静默失效。
- [x] **新增上线前体检 `scripts/check-dist.mjs`。** `cd site && npm run docs:release` = 构建 + 体检。检查三件事：站内链接可达性、产物里是否混入源目录、robots 与 sitemap 是否自洽。当前 62 个文件 / 1.47 MB / 13 页 / 334 条站内链接，零死链。

### 缺陷修复（本轮）

- [x] **`/volumes/` 一直 404。** 根因：`syncDocs()` 每次构建整目录删除 `site/docs/volumes`，而 `volume/index.md` 从未落盘。**修法是让构建器成为唯一真源**：新增 `VOLUMES_OVERVIEW_MD` 常量并在 `syncDocs()` 开头写入。教训：**凡是构建器会删除的目录，其内容必须由构建器自己生成**，否则删除与重建之间必然出现空洞。
- [x] **`Beads` 组件产出死链。** 模板字符串漏了目录名，生成了 `/volumes/01-` 这类不存在的地址。已改为从 `site-index.json` 查 `dir`；无正文的卷降级为非链接 `<span>`，不再假装可点。
- [x] **全站内部链接统一走 `withBase`**（18 处硬编码 `/volumes/...`、`/cases/...`）。此前设置 `DOCS_BASE` 做子路径部署时链接会全断。
- [x] **主题默认排版压过组件样式（一类问题，已统一处理）。** VitePress 的 `.vp-doc h1~h4` 与 `.vp-doc p` 权重为 (0,1,1)，高于组件自己的单类名规则 (0,1,0)。后果：组件标题被塞进 32~48px 上边距、证据抽屉标题被顶出 1px 分隔线与 24px 上内边距、卡片正文行高被拉到 28px、首页大标题的响应式字号失效（390px 下仍按 38px 渲染而折行）、卷卡正文被染成链接蓝。**所有覆盖集中在 `style.css` 同一处并写明成因**；后续新增组件须按同一方式处理。
- [x] **中文 404 页。** 默认主题 404 是英文引文页，改为经 `not-found` 插槽渲染自建组件。注意插槽内容不经过 `.container`，需自带左右内边距。
- [x] **补 favicon。** 此前浏览器标签页为空白（`/favicon.ico` 404）。新增 `site/docs/public/favicon.svg`，与首页主视觉同源：14 卷书脊收敛为 4 根，其中一根用强调色。

### 待验证

- [ ] **深色模式对比度实测。** Token 齐全，已目视验收首页与章页；正文表格与证据抽屉的深色对比度未逐项测量。
- [ ] **真机移动端验收。** 已用 390px 视口逐页确认无横向溢出（含 404 页共 12 个页面）；未在 iOS Safari / Android Chrome 真机验证。
- [ ] **`cleanUrls` 与托管方匹配。** 站点开启 `cleanUrls`，要求托管方把 `/foo` 映射到 `foo.html`。Vercel、Netlify 确有该行为；自建 nginx 需显式配置 `try_files`。**GitHub Pages 不满足**，如选用需先解决链接与产物结构问题。
- [ ] **与 `03-UIUX设计蓝图.md` 的偏差复核。** 首页实际为「入口卡 → 14 卷目录 → 四条路径 → 能力链 → 标记图例」五段，与蓝图中的段数不完全一致，需回写蓝图或确认差异为有意。

### 本机运行状态（2026-09-11 收尾核实）

- [x] **调试产物已清除。** 上一轮用于移动端视口与交互验证的 `_probe4.html`、`_probe5.html`、`_geo2.html`、`_m.html` 曾被直接写进构建产物目录。已通过重新构建清除，产物目录现在只有正式文件，无任何下划线开头的调试页。
- [x] **调试用静态服务已停止。** 此前同时占用两个端口（预览 + 探针）。当前只保留一个预览服务在跑。
- [x] **构建 + 体检复跑通过**：构建 4.68s；产物 62 文件 / 1.47 MB / 13 页 / 334 条站内链接 / 零死链 / 无源目录泄漏。
- [x] **全路由复验。** 16 条路径全部 200（首页、四入口、卷总览、卷页、两章正文、案例库、3 个案例页、favicon、robots、sitemap、检索索引）；未知路径正确落到自建中文 404 页。
- [x] **视觉复核通过。** 首页浅色 / 深色、章页三栏结构均正常；上一轮的主题权重修复稳定生效（卷卡标题 19px、卡标题 17px、正文 16px/30px）。
- [ ] **仍未部署。** 托管方未定，本机运行是当前唯一运行环境。

### 编入台账

- 本轮代码改动：`scripts/build-index.mjs`、`scripts/check-dist.mjs`（新增）、`site/docs/.vitepress/config.mts`、`site/docs/.vitepress/theme/style.css`、`theme/index.ts`、12 个组件、`site/package.json`。
- 本轮新增交付文件：`site/docs/public/favicon.svg`、`vercel.json`、`netlify.toml`、`.vercelignore`、`.gitignore`、`site/README.md`、`scripts/check-dist.mjs`。


---

## 第十轮（2026-09-11）：上游 MDX 组件还原 + 释义机制扩容

### 已关闭（决策）

- [x] **上游 MDX 交互组件不再以裸源码出现在站内。** 快照里的 `<Question choices={[...]} />`、`<NavCard />`、`<StepBar />` 被转义成了字面 JSX 文本，读者看到的是源码而不是内容。构建器新增 `renderMdxComponents()`，按语义降级为普通教材元素：`Question` → 选项列表 + 答案解析（62 处）、`NavCard` → 说明列表（156 处）、`StepBar` → 有序列表（18 处）；`RelatedArticlesSection` / `ChapterIntroduction` / `hfoptions` 属页面外壳，整块删除。
- [x] **`dropVueTemplates` 的开标签正则补 `|$`。** 原写法 `/<[a-z][a-z0-9]*-[a-z0-9-]+(?=[\s/>])/` 在多行组件的开标签位于行尾时匹配不到，导致组件壳残留。判据改为 `(?=[\s/>]|$)`。
- [x] **处理顺序固定为 `wrapBareMarkup(dropVueTemplates(renderMdxComponents(src)))`**，三者有依赖关系，顺序调换会重新出现裸标签。
- [x] **释义槽位从「只认段落」扩到「段落 + 列表 + 引用」。** 教学内容的步骤、要点、自测选项大多以列表承载，只给段落挂释义会漏掉整片。新增 `TR_BLOCK` 与 `TR_MARK`；dump 输出带 `⟨列表⟩` / `⟨引用⟩` 前缀，便于人工核对槽位。
- [x] **新增 `--migrateTranslations=<卷/课程>`。** 槽位口径变化会让旧译文下标整体错位，必须迁移而不是重译。判据是「按块顺序重建」，不是按段落类型猜测。

### 待验证

- [ ] 上游提示词模板里的 `Role:` / `Language:` / `Rules:` 等占位符仍以 `&lt;` 形式保留（属正文，不应清除）。需在抽检时确认它们没有影响可读性。

---

## 第十一轮（2026-09-12）：释义质量与覆盖率

### 已关闭（决策）

- [x] **释义不再重复原文的标记符号。** `tb-zh` 里的内容是作为原始 HTML 块插入 markdown 的，markdown-it 不会二次解析，因此译文里的 `[文字](链接)`、`**加粗**`、反引号会原样显示成源码。新增 `cleanGloss()`，在生成释义时统一去掉链接、加粗、行内代码与行首列表标记，只留下可读正文。
- [x] **已是中文的块不再占释义槽位。** 自测题的「选项」「答案解析」小标题由构建器自己生成，若也分配槽位，读者会在中文小标题下再看到一条一模一样的中文释义。新增 `needsGloss()`：块内不含拉丁字母即不参与释义。槽位口径随之变化，已用 `--migrateTranslations` 迁移。
- [x] **`huggingface-mcp-course` 释义补齐至 100%**：37 篇文档 / 862 个槽位 / 862 条中文释义，全部按上游原文逐段撰写，原文一字未动。

### 编入台账

- 本轮改动：`scripts/build-site-content.mjs`（`cleanGloss`、`needsGloss`、`trBlocks`、`withTranslations`、迁移器）。
- 新增译文：`translations/10-context-memory/huggingface-mcp-course.json`（862 条）。
- 站内核对：MCP 课程 37 个页面共渲染 862 个 `tb-zh` 释义块，与槽位数一致。

### 待验证

- [ ] 全站 98 条英文来源的释义覆盖率仍然很低（当前仅 1 门课程完成）。需要按课程质量排序，确立「先做哪些」的公开顺序，并在课程页标注释义进度，避免读者误以为缺失是故障。

---

## 第十二轮（2026-09-12）：正文覆盖补齐 + 对外发布

### 已关闭（决策）

- [x] **`roots` 口径过窄，整门课只搬了入口。** `microsoft-generative-ai-for-beginners` 只登记了 `00-course-setup`，21 课正文全部留在快照里 —— 与《04-待验证与决策队列》第 48 行「19 课英文正文」的记录不符。已把 22 个课时目录全部列入 `roots`，站内由 5 页增至 32 页。同批修正 `mattpocock-skills`（补 `docs/engineering`、`docs/productivity`，59→85 页）、`agent-systems-handbook`（补 `skills` 技能包，88→201 页）、`how-to-use-dify`（补 `cheatsheets`、`docs/reference`，16→22 页）。
- [x] **来源总表与路径页的「原文」列一直是空的。** 两处循环遍历的是原始 `catalog.sources`，而 `entryUrl` 只存在于派生的 `allSources` 上，判据恒为假。新增 `upstreamUrl(s)`（入口文档 → 仓库首页 → 站点，逐级回退），两处改用它。修复后 **143 条来源全部有可点原文**，路径页补出 29 条外链。
- [x] **6 条网站导出型来源没有任何外链。** 这类来源没有 repo，`site` 字段为空。从各自的 `_快照信息.md` 抽出站点地址写回 `catalog.json`：阿里云千问办公帮助中心、扣子文档、Dify 文档、Manus 文档、WorkBuddy 文档、千问办公学习站。`site` 必须落在 `catalog.json`，因为 `upstream/` 不入库。
- [x] **`openai/skills` 许可降级为「仅引用」。** 该来源的许可按「逐技能 LICENSE.txt，抽样为 Apache-2.0」判成可转载，但逐条核对发现 44 份 `LICENSE.txt` 中有 8 份是 Figma Developer Terms、4 份是 Notion Labs 版权，另有若干 MIT/Apache。混合且含限制性条款，不能整篇转载。同步在 `build-catalog.mjs` 的 `LICENSE_RULES` 里补规则，重新生成时结论一致。
- [x] **单页来源不上架是我方既定口径。** `build-site-content.mjs` 要求 `set.length >= 2`，5 条只有单个 README 的 awesome 清单因此没有 `lib/` 目录。这不是缺陷，已撤销此前的 `readingPlan` 登记，仅保留在来源总表中（仍带原文入口与中文标题）。`docs/public/skills` 同理：构建器会跳过 `public|assets|images` 等静态目录，登记该 root 无效，已撤回。
- [x] **新增 `scripts/build-notice.mjs`。** `NOTICE.md` 此前只能手工维护。改为从 `catalog/catalog.json` 生成：许可分级统计、逐条署名（课程 / 上游 / 锚定版本 / 许可 / 级别 / 站内入口）、仅登记清单、常见许可要点。214 条仓库内相对链接全部可解析。
- [x] **仓库 slug 出现在读者面前。** `mattpocock-skills` 的标题是 `**Matt Pocock Skills**`（markdown 星号原样显示），另有 7 条来源标题直接是 `owner/repo` 或本地目录名。已在 `curation.titles` 里补齐中文标题；已有人工命名的条目保持原样不动。

### 已发布

- [x] **创建公开仓库。** `https://github.com/faruheaisha/ai-native-open-textbook`（MIT，143 条来源 / 107 门课程 / 2500 篇正文）。`upstream/`（1.26 GB 只读快照）与构建缓存不入库，提交体积 47.9 MiB。
- [x] **补齐根级文档。** `README.md`（八条学习路径、十二种资料类型、目录结构、本地运行、许可与署名）、`LICENSE`（标准 MIT 原文，便于平台自动识别；适用范围说明移入 `NOTICE.md` 与 `LICENSES/README.md`）、`NOTICE.md`、`LICENSES/`（MIT 与 Apache-2.0 全文）。
- [x] **仓库 About 写全。** description 概述编排口径与规模，20 个 topics 覆盖 ai / agents / vibe-coding / claude-code / codex / harness / context-engineering / mcp 等。

### 待验证

- [ ] **全量构建的内存占用。** `vitepress build` 在 15.7 GB 机器上多次因 esbuild 服务被 OOM 杀死而中断。内容侧已用 `scripts/check-vue-render.mjs` 排除（2607 页 Vue 编译 0 失败），剩余是环境问题，需要给出可复现的最小内存要求或分批构建方案。
- [ ] **「限非商用」正文进公开仓库的边界。** 9 条 CC BY-NC 来源的正文已随站点进入公开仓库，`NOTICE.md` 已声明仅限非商业教育用途。是否需要为这类内容单独加目录级声明，待定。
- [ ] **`origin-harness-engineering-articles` 的 `site` 指到 Fowler 单篇。** 该来源实为三篇原始文献的合集（Fowler / Mitchell Hashimoto / arXiv 2603.05344），当前只给了 Fowler 一文的地址，其余两篇写在 `licenseNote` 里。若读者反馈误导，改为不设 `site` 或拆成三条来源。
