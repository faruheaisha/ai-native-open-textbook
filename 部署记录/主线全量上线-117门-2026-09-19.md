# 主线课程全量上线记录（117 门）

日期：2026-09-19
范围：补齐每卷主线必读课程的最后缺口，并把全站导航、搜索与原件归档对齐到实际上线集合

## 本次上线课程

- `09-harness/claude-code-book-yuyu`（御舆：解码 Agent Harness，61 页）——主线必读，此前因许可「未声明」未上线，按教育用途决策上线
- `10-context-memory/hf-context-course`（The Context Course，136 页）——同上
- `10-context-memory/awesome-context-engineering`（上下文工程清单）——可转载，增量部署遗漏，补齐

至此 8 卷的主线必读课程 100% 上线；线上合计 117 门 = 113 门可转载 + 4 门仅引用（另两门早期已上线的官方文档）。

## 导航与搜索对齐（本次的核心修复）

线上此前的问题：课程库页停在 112 门、路径页列着未上线课程（死链）、搜索没有新课。根因是主题组件（CourseLibrary / HomeHeroStats / PathGrid 等）直接 import 全量 `generated/catalog.ts`，增量批次构建从未刷新过它们。本次：

- 上线集定义为 117 门，据此生成过滤版目录，构建期间换入 `generated/catalog.ts` 与 `catalog-batch.ts`（config.mts 的三元 import 两个分支都会被 esbuild 静态打包，两个文件必须同时存在），构建后还原完整版。
- `paths/*.md` 移除 19 门未上线课程的表格行，并把「共 X 条来源，其中 Y 门可在站内直接阅读」改写为实际上线数；`library/index.md` 硬编码课程数改为 117。
- 搜索索引按上线集重建：117 门 · 19,665 页 · 12MB。
- 部署工具与验证脚本：`tmp/prep-deploy-0919.mjs`、`tmp/run-build-0919.mjs`、`tmp/push-0919.sh`、`tmp/push-raw-0919.sh`、`tmp/verify-0919.sh`（未入库）。

## raw 原件归档补推

此前服务器从未部署过 `/raw/`，线上每页的「原件 ↓」链接全部 404。本次把 117 门上线课程的原件归档（41,736 个文件，1.3GB）以 tar 流式压缩直推服务器解包。19 门未上线课程的 raw 未推，与页面口径一致。

## 验证证据

- 构建：搜索索引 117 门 / 19,665 页；VitePress 渲染 91s 成功；产物校验含新增检查「课程库 SSR 不含未上线课程链接」。
- HTTPS：首页、课程库、来源总表、关于、8 个路径页、3 门新课入口与课时页、来源详情页全部 200。
- 新课标题：御舆：解码 Agent Harness ｜ The Context Course ｜ Awesome Context Engineering。
- 导航一致性：课程库 + 8 个路径页共引用 117 门，与上线集恰好相等，0 死链、0 遗漏。
- 搜索：3 门新课均被索引，索引课程数 117。
- raw 抽查：3 个样本文件经 HTTPS 取回，sha256 与本地清单一致；服务器 `/raw` 文件计数 41,736，与清单分毫不差。
- `nginx -t` 通过并已 reload。
- 回滚备份：`/var/www/ai-native-textbook-backup-mainline-20260919`（切换前完整站点）。

## 后续

- 剩余 19 门未上线课程全部是「仅引用/未声明许可」的非主线官方文档与社区资料，等待决策：若教育用途需要，可按同一套过滤目录流程（117 → 136）一次补齐。
- 局部提醒：`openai-codex-docs-official`、`openai-plugins-docs` 两门仅引用课程在线上属于早期遗留，与 NOTICE.md 的「仅引用不收录正文」口径存在出入，建议在 curation 或 NOTICE 中补一条教育用途例外说明。
