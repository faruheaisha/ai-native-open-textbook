# 附录 B · 五步排查卡（打印版）

## B.1 打印版排查卡

症状：改了不生效 / 装了没反应 / 行为不对。沿「文件 → 拼装 → 请求」链走：

```
① 层级  你改的在哪个作用域？
        技能两层：~/.workbuddy/skills 与 <项目>/.workbuddy/skills
        规则只对本项目生效；身份不进专家对话（4.8.1 版本修复）

② 开关  它通电了吗？
        grep '"<名字>' ~/.workbuddy/settings.json
        head -5 <技能>/SKILL.md   ← frontmatter 三横线闭合？

③ 拼装  它在这次请求的清单里吗？
        新会话触发一次 → 会话文件里找注入痕迹（专家 system-reminder / 技能读取事件）

④ 缓存  新内容被旧状态压住了吗？
        云端画像覆盖手改；市场更新覆盖模板改动；改配置后重启对照

⑤ 版本  机制变了吗？
        cat ~/.workbuddy/app/renderer-version.json → 对照官方更新日志
```

闭环：修复后**新开会话**验证；旧会话不反映修复。 取证：ls -lt ~/.workbuddy/projects/*/*.jsonl | head -3（最近会话）。
